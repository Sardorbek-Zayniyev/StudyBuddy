{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Django Server",
            "type": "shell",
            "command": "./manage.py",
            "args": ["runserver"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
